apiVersion: v1
kind: Service
metadata:
  name: substratforge
spec:
  selector:
    app: substratforge
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 9090
  type: ClusterIP
