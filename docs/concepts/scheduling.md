# 概念：密度调度

调度器把沙箱分为三层温度：

- **热（hot）**：running，占资源，随时可用
- **温（warm）**：paused，进程驻留内存，秒级恢复
- **冷（cold）**：suspended，快照落盘，运行环境已释放

调度目标：热+温的数量不超过宿主容量，冷停靠数量尽量大。
空闲 -> 温 -> 冷（挂起）；访问 -> 冷 -> 热（恢复）。
