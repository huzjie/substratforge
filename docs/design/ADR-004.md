# ADR-004：增量快照采用 rsync 风格分块

## 状态
已接受（演示级），生产替换 CAS

## 决策
`DeltaEncoder` 用 64KB 分块 + sha256 引用实现块级增量。

## 理由
零依赖、易懂、可跑通；生产升级路径明确（内容寻址存储）。

## 影响
- 增量链父快照受 GC 保护
- 见 `docs/snapshot-cas.md`
