# ADR-002：三档隔离后端而非单一后端

## 状态
已接受

## 决策
提供 process / gvisor / microvm 三档，共享 `SandboxBackend` 接口。

## 理由
不同负载对隔离与开销的权衡不同，单一后端无法覆盖
「本地开发 → 生产多租户 → 高安全」全谱。

## 影响
- 通过 registry 按 `IsolationLevel` 取值
- 每档后端独立模块，可插拔
