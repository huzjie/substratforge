# ADR-003：显式状态机 + 转移表

## 状态
已接受

## 决策
生命周期用显式转移表（`runtime/lifecycle.py` 的 `TRANSITIONS`）约束，
非法迁移抛异常。

## 理由
挂起/恢复链路是核心正确性来源，显式转移表能尽早暴露状态机 bug，
避免「从 destroyed 恢复」这类非法操作。

## 影响
- `Lifecycle` 包装 `StateMachine`
- destroy/failed 为任意态终态，绕过转移表
