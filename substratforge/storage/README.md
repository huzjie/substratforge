# 快照存储

支持四种后端（`storage.backend` 配置）：

| 后端 | 说明 | 适用 |
|---|---|---|
| `local` | 本地文件系统，默认 | 单机、开发 |
| `s3` | AWS S3（签名 V4，零依赖） | 生产、跨机恢复 |
| `minio` | MinIO（S3 兼容） | 自建对象存储 |
| `memory` | 进程内存 | 测试、演示 |

GC 策略见 `gc.py`，支持保留条数 + 超龄回收，且保护增量链父快照。
